# This file is used by Firebase App Hosting to configure the production environment.
# Removing the manual environment variables to rely on automatic configuration.

# The run command to build the app
run: npm run build

# The directory to serve the app from
serve: .next
